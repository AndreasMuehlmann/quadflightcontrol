from abc import ABCMeta, abstractmethod


class InterfaceUser(metaclass=ABCMeta):
    @abstractmethod 
    def send_outputs(outputs)
        pass

    @abstractmethod 
    def give_inputs():
        pass
